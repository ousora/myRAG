# Core dependencies for myRAG pipeline (production)
pymupdf>=1.24              # PDF parsing (fast, supports OCR via PaddleOCR)
python-docx>=1.1           # DOCX parsing
beautifulsoup4>=4.12       # HTML parsing
readability-lxml>=0.7      # HTML content extraction
markdown>=3.5              # Markdown parsing
tiktoken>=0.6              # Token counting for chunking

# Optional (install via extras)
paddleocr>=2.8             # Image OCR — install with: pip install myrag[pdf]
